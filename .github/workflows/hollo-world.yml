name: Hello World
on:
  - push
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello, world!"
